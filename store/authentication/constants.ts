export const AUTHENTICATION_ACTION_PREFIX = 'authentication.';

export const AUTHENTICATION_SUCCEED_TYPE = `${AUTHENTICATION_ACTION_PREFIX}succeed.type`;
export const LOGOUT_TYPE = `${AUTHENTICATION_ACTION_PREFIX}logout.type`;
