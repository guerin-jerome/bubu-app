import React from 'react';
import {SafeAreaView, ScrollView, StatusBar} from 'react-native';

function App() {
  return (
    <SafeAreaView>
      <StatusBar />
      <ScrollView />
    </SafeAreaView>
  );
}

export default App;
